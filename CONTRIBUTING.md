# 贡献指南

感谢你愿意为这个项目贡献代码、文档或其他形式的帮助!本文档将帮助你了解如何参与贡献。首次贡献前,请花几分钟阅读全文。

---

## 目录

- [行为准则](#行为准则)
- [如何参与贡献](#如何参与贡献)
  - [报告 Bug](#报告-bug)
  - [提出新功能建议](#提出新功能建议)
  - [完善文档](#完善文档)
  - [提交代码](#提交代码)
- [环境搭建](#环境搭建)
- [开发流程](#开发流程)
- [代码规范](#代码规范)
- [提交信息规范](#提交信息规范)
- [Pull Request 流程](#pull-request-流程)
- [社区沟通](#社区沟通)

---

## 行为准则

我们希望每一位参与者都能在一个友好、尊重、包容的环境中交流。请遵守以下基本原则:

- **尊重他人**:不使用攻击性、歧视性或侮辱性语言。
- **就事论事**:批评针对的是代码或观点,而非个人。
- **协作共赢**:主动帮助他人,分享知识。

---

## 如何参与贡献

贡献不只有写代码这一种方式,以下任何形式都同样有价值。

### 报告 Bug

如果你发现了 Bug,请通过 [Issue](https://github.com/<用户名>/<仓库名>/issues) 提交。一个好的 Bug 报告应包含:

1. **清晰的标题**:一句话描述问题,例如「`login` 函数在空输入时崩溃」。
2. **复现步骤**:尽可能详细地列出触发问题的操作顺序。
3. **期望行为 vs 实际行为**:说明你期望发生什么,以及实际发生了什么。
4. **环境信息**:
   - 操作系统及版本
   - 运行时版本(如 Python / Node.js / Go 等)
   - 项目版本或提交哈希(`git rev-parse HEAD`)
5. **相关日志或截图**:粘贴报错信息、堆栈跟踪或截图。

> 在提交前,请先搜索已有 Issue,避免重复报告。

### 提出新功能建议

提出新功能前,建议先发起讨论,确认该功能与项目方向一致。提交时请说明:

- **背景**:你遇到了什么问题,为什么需要这个功能?
- **期望方案**:你希望以什么方式解决。
- **备选方案**:你考虑过的其他实现方式,以及它们的优劣。

### 完善文档

文档改进(修正错别字、补充示例、翻译等)是最容易上手的贡献方式。即使是小的修正也很有价值。

### 变更范围与提前沟通

在动手之前,请先判断你的改动属于哪一类——不同规模的改动,对流程的要求不一样:

- **可以直接提 PR 的小改动**:修小Bug、改文档、修正翻译、小的兼容性修复。这类改动边界清晰、风险可控,无需事先说明,直接按下面的流程提交即可。
- **需要先沟通的改动**:新功能、重构、架构调整,以及会触及核心逻辑(例如角色或任务行为)或明显改变现有功能体验的修改。这类改动影响面较大,请在开始开发前先联系作者或在社区发起讨论,讲清楚你想解决什么问题、打算怎么做,方向确认后再动手或提交,以免白费功夫。
- **拿不准的改动**:如果你不确定自己的修改是否属于上面所说的「大改」,保守起见,先联系作者确认,再决定是否开始。

提前沟通并不是设门槛,而是为了让你和项目都少走弯路——方向一致,你的贡献才更可能被顺利合入。

### 提交代码

如果你准备提交代码,请按以下流程操作(详见后续章节):

1. Fork 本仓库。
2. 创建功能分支。
3. 编写代码并补充测试。
4. 提交 PR。

---

## 环境搭建

### 前置要求

- [Git](https://git-scm.com/)
- uv 及 `pyproject.toml` 要求的 Python 运行环境

### 获取代码

```bash
# 1. Fork 本仓库(在 GitHub 页面点击 Fork 按钮)

# 2. 克隆你 Fork 的仓库
git clone https://github.com/<你的用户名>/<仓库名>.git
cd <仓库名>

# 3. 添加上游仓库,便于同步更新
git remote add upstream https://github.com/<用户名>/<仓库名>.git
```

### 安装依赖

当前依赖以 `pyproject.toml` 为准，使用 uv 管理，不新增 requirements 文件作为第二套依赖来源。命令找不到时先检查 PATH 或使用已确认的绝对路径，不直接判断未安装。具体步骤见[运行环境规范](docs/development/运行环境与命令执行.md)。

```powershell
uv sync
```

### 运行测试

Python 脚本通过 `uv run --no-sync python <脚本路径>` 执行，模块通过 `uv run --no-sync python -m <模块名>` 执行；将占位内容替换为实际目标。先确认相关测试的依赖与副作用，不能直接批量运行会接管键鼠的脚本，也不假定测试框架已经安装。

---

## 开发流程

1. **检查分支与工作区**:先确认已有改动和当前分支，再根据任务需要同步上游；不要自动切换分支或覆盖用户工作。以下同步命令仅在确认分支与工作区允许后执行。

   ```bash
   git checkout main
   git pull upstream main
   ```

2. **创建功能分支**:每个 PR 对应一个独立分支,分支名使用简短、描述性的名称。

   ```bash
   git checkout -b feat/add-xxx
   git checkout -b fix/xxx-bug
   ```

3. **编写代码**:遵循[代码规范](#代码规范),为新增或修改的功能补充测试。

   使用 AI 辅助开发时，贡献者必须先阅读[AI 开发规范与改动总则](docs/development/AI开发规范与改动总则.md)，并明确要求 AI 在修改前阅读总则及与本次任务相关的专题文档。不能假定 AI 会自动加载这些文件；贡献者仍需审查生成内容，确认符合规范并完成必要验证。

4. **本地验证**:提交前运行测试和静态检查,确保通过。

   测试按上文选取实际入口；对涉及的文件运行已配置的静态检查，例如：

   ```powershell
   uv run --no-sync python -m ruff check <修改的文件路径>
   ```

5. **提交并推送**:

   ```bash
   git add <本次修改的文件路径>
   git commit -m "feat: add xxx feature"
   git push origin feat/add-xxx
   ```

---

## 代码规范

请尽量遵循以下约定(如已有具体规范,以项目内配置为准):

详细要求见[开发规范入口](docs/development/AI开发规范与改动总则.md)，包括中文注释与 Google 风格 docstring、单次固定值内联、已有逻辑复用、状态建模和日志分级。

- **可读性优先**:变量、函数命名清晰,必要时添加注释说明「为什么」而非「做了什么」。
- **保持一致性**:遵循项目现有的代码风格,不要混用不同风格。
- **职责清晰**:连续逻辑直接表达，不为缩短函数机械拆分，不增加无用抽象层。
- **避免重复**:优先复用已有实现，相同业务规则集中维护，不仅因代码形似而强行抽象。
- **妥善处理错误**:不吞掉异常,给用户清晰的错误提示。
- **补充测试**:新功能应有对应测试,修复 Bug 应添加能复现该 Bug 的回归测试。
- **不提交无关文件**:避免提交调试代码、临时文件、编译产物等,必要时添加 `.gitignore`。

---

## 提交信息规范

建议使用 [Conventional Commits](https://www.conventionalcommits.org/zh-hans/) 规范,使提交历史清晰可读、便于自动生成变更日志。

格式:`<类型>(<范围>): <描述>`

常用类型:

| 类型       | 说明                         |
| ---------- | ---------------------------- |
| `feat`     | 新增功能                     |
| `fix`      | 修复 Bug                     |
| `docs`     | 文档变更                     |
| `style`    | 代码格式(不影响逻辑)         |
| `refactor` | 重构(既非新功能也非修 Bug)   |
| `perf`     | 性能优化                     |
| `test`     | 新增或修改测试               |
| `build`    | 构建系统或依赖变更           |
| `ci`       | CI 配置变更                  |
| `chore`    | 其他不影响源码的杂项         |

示例:

```bash
git commit -m "feat: add login authentication"
git commit -m "fix(parser): handle empty input gracefully"
git commit -m "docs: update installation guide"
```

---

## Pull Request 流程

具体的提交范围、标题正文、验证证据和发布边界见[提交与 PR 规范](docs/development/提交与PR规范.md)。以下步骤按用户授权和实际任务范围执行，文档编辑本身不意味着需要推送或创建 PR。

1. **推送分支后**,在 GitHub 上点击「Compare & pull request」。
2. **填写 PR 描述**,包含以下内容:
   - 变更的动机和背景
   - 变更内容摘要
   - 关联的 Issue 编号(如 `Closes #12`)
   - 测试情况说明
3. **保持 PR 精简**:一个 PR 尽量只解决一个问题,便于审查和合并。
4. **响应审查意见**:维护者或社区成员可能会提出修改建议,请及时跟进。如有分歧,友好沟通、说明理由。
5. **通过 CI**:确保自动测试和检查全部通过。
6. **等待合并**:由维护者负责合并。若 PR 长期未处理,可在评论区礼貌提醒。

### 提交 PR 前检查清单

- [ ] 代码能通过所有质量检测测试
- [ ] 已补充必要的实机测试，并进行人工审查(非纯AI审查,AI做不到的)确认实现无误
- [ ] 已遵循代码规范和提交信息规范
- [ ] 已同步上游最新代码并解决冲突
- [ ] PR 描述清晰完整

---

## 社区沟通

- **问题讨论**:优先在 [Issue](https://github.com/<用户名>/<仓库名>/issues) 中公开讨论,方便其他人参考。
- **提问方式**:提问前先查阅文档和已有 Issue;提问时提供尽可能完整的上下文。
- **响应时间**:本仓库志愿维护,可能无法即时回复,请耐心等待。

---

再次感谢你的贡献!每一份帮助,无论大小,都让这个项目变得更好。
